name: Deploy VaultUI Pages

on:
  push:
    branches:
      - main
    paths:
      - "web/**"
      - "Libraries/SynergyUI/**"
      - "Libraries/Bacon/**"
      - "Libraries/Armenta-Lib/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Assemble static site
        shell: bash
        run: |
          set -euo pipefail
          rm -rf _site
          mkdir -p _site/Libraries/SynergyUI _site/Libraries/Bacon _site/Libraries/Armenta-Lib
          cp -R web/. _site/
          cp Libraries/SynergyUI/source.lua Libraries/SynergyUI/example.lua _site/Libraries/SynergyUI/
          cp Libraries/SynergyUI/preview.html _site/Libraries/SynergyUI/
          cp Libraries/Bacon/source.lua Libraries/Bacon/example.lua Libraries/Bacon/preview.html _site/Libraries/Bacon/
          cp Libraries/Armenta-Lib/source.lua Libraries/Armenta-Lib/preview.html _site/Libraries/Armenta-Lib/
          touch _site/.nojekyll

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to Pages
        id: deployment
        uses: actions/deploy-pages@v4
